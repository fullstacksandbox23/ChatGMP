name: chatgmp_dev
services:
  chatgmp:
    build:
    env_file:
      - .env
    ports:
      - "5001:80"
    volumes:
      - dev-app-data:/data
      - ./chatgmp/:/app
    restart: unless-stopped
    networks:
      - dev_network

volumes:
  dev-app-data:

networks:
  dev_network:
